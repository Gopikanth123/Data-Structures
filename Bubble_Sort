def bubble_sort(arr):
    n=len(arr)
    for i in range(n):
        for j in range(n-i-1):
            if arr[j]>arr[j+1]:
                temp=arr[j]
                a[j]=a[j+1]
                a[j+1]=temp
    return arr

a = [int(input()) for i in range(int(input("Enter the length of the array: ")))]
print(bubble_sort(a))
